#include <iostream>
#include <cmath>

using namespace std;

int main()
{
    int a;
    int b;
    cin >> a;
    cin >> b;
    int c = sqrt(pow(a,2) + pow(b,2));
    cout << c;
    return 0;
}
